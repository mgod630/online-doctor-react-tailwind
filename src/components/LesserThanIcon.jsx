import classNames from "classnames";

const LesserThanIcon = ({ className }) => {
    return (
        <p className={classNames("text-blue-600 text-xl", className)}>&lt;</p>
    );
};
export default LesserThanIcon;
